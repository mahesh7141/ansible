---
- name: install sw apps
  hosts: all
  tasks:
    - name: install s/w's
      apt:
          name: "{{item}}"
          state: present
          update_cache: yes
      with_items:
           - tree
           - maven
           - git
...
